01) ORGANIZANDO OS COMPONENTES POR PASTAS:

    AO INVES DE CRIARMOS OS COMPONENTES DIRETAMENTE 
    DENTRO DA PASTA COMPONENTS, 
    IREMOS SEPARAR NOSSOS COMPONENTES EM SUAS RESPECTIVAS PASTAS.

    A SEGUIR IREMOS CRIAR UM COMPONENTE QUE FICARA DENTRO DA PASTA PROFILE. 

02) DENTRO DA PASTA PROFILE IREMOS CRIAR UM ARQUIVO CHAMADO INDEX.JS, 
    QUE POSTERIORMENTE PODERA SER IMPORTADO PARA QUALQUER PAGINA.

03) DENTRO DO ARQUIVO INDEX.JS, 
    IMPORTAREMOS O REACT 

    import React from 'react'

04) AGORA VAMOS CRIAR UMA CONSTANTE CONTENDO UMA ARROW, 
    ESSA CONSTANTE IRA TER O NOME DO COMPONENTE QUE ESTA SENDO CRIADO:

    const Profile = () => (
      
    )

05) DENTRO DA CONSTANTE SERA DEFINIDA A 
    ESTRUTURA DO COMPONENTE:

    const Profile = () => (
      <div className="Profile-wrapper">
        <h1>Yago Milano</h1>
        <h2>Desenvolvedor Front End</h2>
        <p>Membro do Squad1 de desenvolvido da Vnda, escrevo sobre o mundo front end, viagens, vida pessoal e mais.</p>
      </div>
    )

    export default Profile

06) O COMPONENTE RESPONSAVEL POR ESTRUTURAR/AGRUPAR TODAS AS PAGINAS 
    FICA NO ARQUIVO LAYOUT.JS

    COMO O COMPONENTE QUE ESTAMOS CRIANDO SERA EXIBIDO DENTRO DE UM ASIDE QUE 
    SERA COMPARTILHADO/UTILIZADO EM TODAS AS DEMAIS PAGINAS DA APLICACAO, 
    IREMOS CRIAR UM ASIDE DENTRO DO ARQUIVO LAYOUT.JS

    AGORA IREMOS IMPORTAR O COMPONENTE PROFILE PARA INSERIR-LO 
    DENTRO DESSE ASIDE:

    import Profile from './Profile'

07) AGORA PERCEBEREMOS ALGO ESTRANHO. 
    NA PAGINA HOME NOS ESTAREMOS VISUALIZANDO O COMPONENTE PROFILE, 
    MAS NA PAGINA ABOUT, NAO. 

    ISSO ESTA ACONTECENDO PORQUE PRECISAREMOS 
    SEGUIR OS SEGUINTES PASSOS:

    PRIMEIRAMENTE IREMOS IMPORTAR OS COMPONENTES LAYOUT E SEO 
    PARA DENTRO DA PAGINA ABOUT.JS, 
    JA QUE É NELA QUE IREMOS EXIBIR O COMPONENTE PROFILE.

    import Layout from '../components/layout'
    import SEO from '../components/seo'

08) AGORA IREMOS IMPORTAR ESSES COMPONENTES PARA DENTRO 
    DA CONSTANTE ABOUT.

    AO FAZER ISSO,
    O COMPONENTE PROFILE IRA SER EXIBIDO AUTOMATICAMENTE NA PAGINA ABOUT

    import Layout from '../components/layout'
    import SEO from '../components/seo'

    const AboutPage = () => (
      <Layout>
        <SEO title="About"/>
        <h1>About Page</h1>
      </Layout>
    )

0